# 项目笔记（project-notes）

本目录汇集 odoo19 / lawgraph 相关的规划文档与跨会话记忆，从 Claude Code 的
`~/.claude/plans/` 和 `~/.claude/projects/.../memory/` 同步而来，纳入仓库做版本管理。

- `agile-gliding-quail.md` — Agent Memory 独立服务设计 + 进度快照 + odoo-ai 分析 + VexDB-Lite 选型决策
- `memory/` — 跨会话记忆条目（SeekDB 配置、SAG 经验、架构决策等），`MEMORY.md` 为索引

> 注意：此为文档快照，源头仍在 `~/.claude`。更新时从源同步，勿在此直接编辑作为唯一副本。
